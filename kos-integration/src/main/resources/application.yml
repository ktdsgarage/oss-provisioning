server:
  port: 8084
  servlet:
    context-path: /api

spring:
  application:
    name: kos-integration
  datasource:
    url: jdbc:postgresql://localhost:5432/kos_db
    username: postgres
    password: oss-pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: kos-groupe
